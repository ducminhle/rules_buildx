# toolchain

This example demonstrates how BuildX can be used as a tool inside of a `genrule`.

Note the trick of piping in a `.tar` as context; it can be used to position files as needed by a Dockerfile.
